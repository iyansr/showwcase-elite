import Box from '@showwcase/components/basic/Box';
import Button from '@showwcase/components/basic/Button';

const HomePage = () => {
  return (
    <Box px={4} mx="auto" bg="red" maxWidth={100}>
      Hello World
      <Button>aaa</Button>
    </Box>
  );
};

export default HomePage;
